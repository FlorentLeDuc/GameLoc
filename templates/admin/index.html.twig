{% extends 'base.html.twig' %}


{% block body %}
<div class="container">
    <h1>Bonjour, {{ controller_name }}! ✅</h1>
    <p>Içi vous pouvez gérer tout les aspects du sites</p>
    <a class="btn btnadmin" href="{{ path('user_index') }}">Gère tes utilisateurs</a>
    <a class="btn btnadmin" href="{{ path('category_index') }}">Gère les catégories</a>
    <a class="btn btnadmin" href="{{ path('offer_index') }}">Gère les annonces</a>
    <a class="btn btnadmin" href="{{ path('game_index') }}">Gère la liste de jeux</a>
</div>


<div>

</div>
{% endblock %}